# Pinned to a known-good combo for Python 3.11 on macOS arm64.
# The pins matter — without them pip's resolver backtracks through pandas 3.x
# / numpy 2.x / numexpr sdist-only versions and the install fails.

# --- Web framework ---
fastapi>=0.110,<0.120
uvicorn[standard]>=0.27,<0.40
python-multipart>=0.0.9
aiofiles>=23.2
python-dotenv>=1.0

# --- Facial Action Coding ---
py-feat==0.6.2              # py-feat: FACS AU detector
mediapipe>=0.10.9,<0.10.15  # later mediapipe pulls protobuf 5 + jax — overkill

# --- Video & numerics — the pins below are the load-bearing ones ---
# numpy is pinned to exactly 1.23.5: it's the only version that satisfies the
# joint constraints of py-feat→nltools (<1.24) and opencv (>=1.23.5).
numpy==1.23.5
pandas>=2.0,<2.2            # newer pandas needs numpy>=1.26
scipy>=1.10,<1.12           # scipy ≥1.12 needs numpy ≥1.24
numexpr==2.8.4              # only <2.8.5 version with arm64 wheel
opencv-python-headless>=4.9,<4.11
ffmpeg-python>=0.2.0
# PyAV: preferred decode path for variable-frame-rate video (gives correct
# per-frame timestamps via best_effort_timestamp). The extractor falls back
# to OpenCV if PyAV isn't installed, so this remains technically optional.
av>=11.0,<13.0

# --- API models ---
pydantic>=2.6,<3.0

# --- Runtime ---
torch>=2.1,<2.5             # newer torch pulls in massive deps; 2.4 is plenty
Pillow>=10.0,<12.0
tqdm>=4.66

# ============================================================================
# Sonic Rebellion (audio pipeline) — sibling to the facial pipeline.
#
# librosa + soundfile are REQUIRED for the audio routes to work at all.
# Everything below them is OPTIONAL — each module degrades to a librosa-
# based "lite" path when its heavy ML dep is missing. Install whichever
# you need; the pipeline will detect them automatically.
# ============================================================================
librosa>=0.10,<0.11
soundfile>=0.12

# --- Optional heavy ML deps (uncomment what you need) ---
#
# Instagram fetcher (profile URL / reel URLs modes):
# instaloader>=4.10,<5.0
#
# Source separation (Spleeter — TensorFlow-based, ~1 GB model):
# spleeter==2.3.2
#
# Speech transcription (Whisper tiny — 39 M params, ~75 MB):
# openai-whisper>=20231117
#
# Pitch tracking (CREPE — better on compressed AAC than librosa.pyin):
# crepe>=0.0.14
# resampy>=0.4.2
#
# Audio-text embedding (CLAP — novelty search against trending pool):
# msclap>=1.3.3
# faiss-cpu>=1.7,<2.0
# pyacoustid>=1.3      # chromaprint fingerprinting (also needs libchromaprint)
#
# Genre + beat (Essentia + madmom — ~500 MB of model weights):
# essentia-tensorflow>=2.1b6
# madmom>=0.16.1
